/**
 ******************************************************************************
 * @file    PIN_CONFIG_SUMMARY.c
 * @author  Chu Yaozong
 * @date    2026-01-15
 * @brief   STM32F103C8 GPIO引脚配置快速参考表
 * @note    此文件仅为配置参考，不包含可执行代码
 ******************************************************************************
 */

/*******************************************************************************
 * 【STM32F103C8 GPIO引脚配置总览】
 *
 * 总引脚数: 35个 (GPIOA: 16 + GPIOB: 16 + GPIOC: 3)
 * 已使用引脚: 13个 (GPIOA: 5 + GPIOB: 8)
 * 未使用引脚: 8个 (已配置为模拟输入模式)
 * 保留/调试引脚: 10个 (不建议配置)
 * USB引脚: 2个 (PA11, PA12 - 未配置)
 *
 * 【重要说明】
 * USART1 已重映射到 PB6/PB7，原 PA9/PA10 未使用
 ******************************************************************************/

/*******************************************************************************
 * 【GPIOA - 引脚配置表】
 ******************************************************************************/
/*
 * PA0  ✅ 定位孔传感器 (INDEX_HOLE_PIN)       - 浮空输入      - Sensor模块
 * PA1  ✅ 芯片检测传感器 (CHIP_DETECT_PIN)    - 浮空输入      - Sensor模块
 * PA2  ✅ 按键-确认 (key_enter)             - 上拉输入      - Key_multi模块
 * PA3  ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PA4  ✅ ADC1采集                        - 模拟输入      - ADC模块
 * PA5  ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PA6  ⚠️ 未使用 (USART1重映射后空闲)       - 模拟输入      - GPIO_Config模块
 * PA7  ⚠️ 未使用 (USART1重映射后空闲)       - 模拟输入      - GPIO_Config模块
 * PA8  ✅ 蜂鸣器 (BUZZER_PIN)              - 推挽输出      - Buzzer模块
 * PA9  ⚠️ 未使用 (USART1已重映射到PB6)     - 模拟输入      - GPIO_Config模块
 * PA10 ⚠️ 未使用 (USART1已重映射到PB7)     - 模拟输入      - GPIO_Config模块
 * PA11 ⏸️ USB_DM                          - USB功能       - 未配置
 * PA12 ⏸️ USB_DP                          - USB功能       - 未配置
 * PA13 🔒 SWDIO (调试接口)                - 调试接口      - 不配置
 * PA14 🔒 SWCLK (调试接口)                - 调试接口      - 不配置
 * PA15 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 */

/*******************************************************************************
 * 【GPIOB - 引脚配置表】
 ******************************************************************************/
/*
 * PB0  ✅ 按键-向上 (key_up)               - 上拉输入      - Key_multi模块
 * PB1  ✅ 按键-向下 (key_down)             - 上拉输入      - Key_multi模块
 * PB2  🔒 BOOT1 (启动模式)                - 复位引脚      - 不配置
 * PB3  🔒 SPI1_SCK / JTDO                - 调试接口      - 不配置
 * PB4  🔒 SPI1_MISO / NJTRST             - 调试接口      - 不配置
 * PB5  ✅ DHT11数据线                     - 推挽输出      - DHT11模块
 * PB6  ✅ USART1_TX (重映射)             - 复用推挽输出  - USART1模块
 * PB7  ✅ USART1_RX (重映射)             - 浮空输入      - USART1模块
 * PB8  ✅ OLED_SCL (I2C时钟)             - 开漏输出      - OLED模块
 * PB9  ✅ OLED_SDA (I2C数据)             - 开漏输出      - OLED模块
 * PB10 ✅ 按键-返回 (key_back)            - 上拉输入      - Key_multi模块
 * PB11 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PB12 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PB13 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PB14 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 * PB15 ⚠️ 未使用                          - 模拟输入      - GPIO_Config模块
 */

/*******************************************************************************
 * 【GPIOC - 引脚配置表】
 ******************************************************************************/
/*
 * PC13 ✅ LED (系统状态指示)              - 推挽输出      - main.c
 * PC14 🔒 OSC32_IN (RTC晶振)             - 复位引脚      - 不配置
 * PC15 🔒 OSC32_OUT (RTC晶振)            - 复位引脚      - 不配置
 */

/*******************************************************************************
 * 【GPIOA - 引脚配置表】
 ******************************************************************************/
/* 
 * PA0  ✅ 定位孔传感器 (INDEX_HOLE_PIN)       - 输入模式    - Sensor模块
 * PA1  ✅ 芯片检测传感器 (CHIP_DETECT_PIN)    - 输入模式    - Sensor模块
 * PA2  ✅ 按键-确认 (key_enter)             - 输入模式    - Key_multi模块
 * PA3  ✅ USART1_RX                         - 复用模式    - USART1模块
 * PA4  ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PA5  ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PA6  ✅ USART1_CK                        - 复用模式    - USART1模块
 * PA7  ✅ USART1_TX                        - 复用模式    - USART1模块
 * PA8  ✅ 蜂鸣器 (BUZZER_PIN)              - 输出模式    - Buzzer模块
 * PA9  ✅ USART1_TX                        - 复用模式    - USART1模块
 * PA10 ✅ USART1_RX                        - 复用模式    - USART1模块
 * PA11 ⏸️ USB_DM                          - USB功能     - 未配置
 * PA12 ⏸️ USB_DP                          - USB功能     - 未配置
 * PA13 🔒 SWDIO (调试接口)                - 调试接口    - 不配置
 * PA14 🔒 SWCLK (调试接口)                - 调试接口    - 不配置
 * PA15 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 */

/*******************************************************************************
 * 【GPIOB - 引脚配置表】
 ******************************************************************************/
/*
 * PB0  ✅ 按键-向上 (key_up)               - 输入模式    - Key_multi模块
 * PB1  ✅ 按键-向下 (key_down)             - 输入模式    - Key_multi模块
 * PB2  🔒 BOOT1 (启动模式)                - 复位引脚    - 不配置
 * PB3  🔒 SPI1_SCK / JTDO                - 调试接口    - 不配置
 * PB4  🔒 SPI1_MISO / NJTRST             - 调试接口    - 不配置
 * PB5  🔒 SPI1_MOSI                      - 调试接口    - 不配置
 * PB6  ⏸️ I2C1_SCL / TIM4_CH1            - 未使用      - 未配置
 * PB7  ⏸️ I2C1_SDA / TIM4_CH2            - 未使用      - 未配置
 * PB8  ✅ OLED_SCL (I2C时钟)             - 复用模式    - OLED模块
 * PB9  ✅ OLED_SDA (I2C数据)             - 复用模式    - OLED模块
 * PB10 ✅ 按键-返回 (key_back)            - 输入模式    - Key_multi模块
 * PB11 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PB12 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PB13 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PB14 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 * PB15 ⚠️ 未使用                          - 模拟输入    - GPIO_Config模块
 */

/*******************************************************************************
 * 【GPIOC - 引脚配置表】
 ******************************************************************************/
/*
 * PC13 ✅ LED (系统状态指示)              - 输出模式    - main.c
 * PC14 🔒 OSC32_IN (RTC晶振)             - 复位引脚    - 不配置
 * PC15 🔒 OSC32_OUT (RTC晶振)            - 复位引脚    - 不配置
 */

/*******************************************************************************
 * 【可复用的未使用引脚列表】
 ******************************************************************************/
/*
 * 当前可复用的引脚（已配置为模拟输入，需重新配置）:
 *
 * 【GPIOA】
 *   - PA3  (模拟输入)
 *   - PA5  (模拟输入)
 *   - PA6  (模拟输入) - 原USART1_CK，USART1已重映射
 *   - PA7  (模拟输入) - 原USART1_TX，USART1已重映射
 *   - PA9  (模拟输入) - 原USART1_TX，USART1已重映射到PB6
 *   - PA10 (模拟输入) - 原USART1_RX，USART1已重映射到PB7
 *   - PA15 (模拟输入)
 *
 * 【GPIOB】
 *   - PB11 (模拟输入)
 *   - PB12 (模拟输入)
 *   - PB13 (模拟输入)
 *   - PB14 (模拟输入)
 *   - PB15 (模拟输入)
 *
 * 【总计】: 12个引脚可复用
 *
 * 【注意】: PA4用于ADC采集，不应复用
 */

/*******************************************************************************
 * 【引脚复用示例代码】
 ******************************************************************************/
/*
 * 示例1: 使用PA4作为LED控制引脚
 * ------------------------------------
 * GPIO_Config_Pin(GPIOA, GPIO_Pin_4, GPIO_Mode_Out_PP);
 * GPIO_WriteBit(GPIOA, GPIO_Pin_4, Bit_SET);  // LED亮
 * GPIO_WriteBit(GPIOA, GPIO_Pin_4, Bit_RESET); // LED灭
 * 
 * 示例2: 使用PB11作为按键输入（上拉）
 * ------------------------------------
 * GPIO_Config_Pin(GPIOB, GPIO_Pin_11, GPIO_Mode_IPU);
 * if(GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_11) == 0)
 * {
 *     // 按键按下
 * }
 * 
 * 示例3: 使用PA5作为PWM输出（需定时器配置）
 * ------------------------------------
 * GPIO_Config_Pin(GPIOA, GPIO_Pin_5, GPIO_Mode_AF_PP);
 * TIM2->CCR3 = 500; // 设置PWM占空比
 * 
 * 示例4: 使用PB12作为外部中断输入
 * ------------------------------------
 * GPIO_Config_Pin(GPIOB, GPIO_Pin_12, GPIO_Mode_IPU);
 * EXTI_InitTypeDef EXTI_InitStructure;
 * EXTI_InitStructure.EXTI_Line = EXTI_Line12;
 * EXTI_InitStructure.EXTI_Mode = EXTI_Mode_Interrupt;
 * EXTI_InitStructure.EXTI_Trigger = EXTI_Trigger_Falling;
 * EXTI_InitStructure.EXTI_LineCmd = ENABLE;
 * EXTI_Init(&EXTI_InitStructure);
 */

/*******************************************************************************
 * 【未来扩展建议】
 ******************************************************************************/
/*
 * 可用的硬件外设接口（对应引脚）:
 *
 * 【I2C1】
 *   - PB6 (SCL) - 已被USART1_TX重映射占用
 *   - PB7 (SDA) - 已被USART1_RX重映射占用
 *   - 如需使用I2C1，需禁用USART1重映射或更换引脚
 *
 * 【I2C2】
 *   - PB10 (SCL) - 已被按键占用 (PB10为返回键)
 *   - PB11 (SDA) - 未使用，可复用
 *
 * 【SPI1】
 *   - PB3 (SCK), PB4 (MISO), PB5 (MOSI) - 调试接口/已占用
 *   - PB5已被DHT11占用
 *   - 需禁用JTAG后PB3, PB4可用
 *
 * 【SPI2】
 *   - PB13 (SCK), PB14 (MISO), PB15 (MOSI) - 全部可复用
 *
 * 【USART1】
 *   - 当前配置: PB6 (TX), PB7 (RX) - 重映射模式
 *   - 原始引脚: PA9 (TX), PA10 (RX) - 已空闲
 *   - 如需恢复原始配置，禁用重映射即可
 *
 * 【USART2】
 *   - PA2 (TX) - 已被按键占用 (PA2为确认键)
 *   - PA3 (RX) - 未使用，可复用
 *
 * 【USART3】
 *   - PB10 (TX) - 已被按键占用 (PB10为返回键)
 *   - PB11 (RX) - 未使用，可复用
 *
 * 【TIM2】
 *   - PA0 (CH1), PA1 (CH2) - 已被传感器占用
 *   - PA2 (CH3) - 已被按键占用
 *   - PA3 (CH4) - 未使用，可复用
 *   - PA15 (CH1_ETR) - 未使用，可复用
 *
 * 【TIM3】
 *   - PA6 (CH1), PA7 (CH2) - 未使用 (USART1重映射后空闲)
 *   - PB0 (CH3), PB1 (CH4) - 已被按键占用
 *
 * 【TIM4】
 *   - PB6 (CH1), PB7 (CH2) - 已被USART1重映射占用
 *   - PB8 (CH3), PB9 (CH4) - 已被OLED占用 (I2C模式)
 *
 * 【ADC1】
 *   - PA0 (通道0), PA1 (通道1) - 已被传感器占用
 *   - PA2 (通道2) - 已被按键占用
 *   - PA3 (通道3) - 未使用，可复用
 *   - PA4 (通道4) - 已被ADC模块使用
 *   - PA5 (通道5) - 未使用，可复用
 *   - PA6 (通道6), PA7 (通道7) - 未使用 (USART1重映射后空闲)
 *   - PB0 (通道8), PB1 (通道9) - 已被按键占用
 *
 * 【可用ADC通道】: PA3, PA5, PA6, PA7
 */

/*******************************************************************************
 * 【配置优先级】
 ******************************************************************************/
/*
 * 1. 保留引脚（不应配置）:
 *    - PC13, PC14, PC15 (LED和RTC晶振)
 *    - PB2 (BOOT1)
 *    - PA13, PA14 (SWD调试接口)
 *    - PB3, PB4 (JTAG调试接口)
 *
 * 2. 已使用引脚（由各模块配置）:
 *    - Sensor模块: PA0, PA1
 *    - Key_multi模块: PA2, PB0, PB1, PB10
 *    - USART1模块: PB6 (TX), PB7 (RX) - 重映射模式
 *    - Buzzer模块: PA8
 *    - OLED模块: PB8, PB9 (I2C模式)
 *    - DHT11模块: PB5
 *    - ADC模块: PA4
 *    - main.c: PC13
 *
 * 3. 未使用引脚（GPIO_Config模块配置为模拟输入）:
 *    - PA3, PA5, PA6, PA7, PA9, PA10, PA15
 *    - PB11, PB12, PB13, PB14, PB15
 *
 * 4. 可选引脚（根据需求配置）:
 *    - PA11, PA12 (USB功能)
 *
 * 【重要】USART1重映射说明:
 *    - USART1已通过AFIO重映射到PB6/PB7
 *    - 原始引脚PA9/PA10现在空闲
 *    - 重映射代码: GPIO_PinRemapConfig(GPIO_Remap_USART1, ENABLE)
 *    - 如需恢复原始引脚，禁用重映射即可
 */

/*******************************************************************************
 * 【低功耗配置建议】
 ******************************************************************************/
/*
 * 为进一步降低功耗，可以配置以下引脚:
 * 
 * 1. USB引脚（不使用USB时）:
 *    GPIO_Config_Pin(GPIOA, GPIO_Pin_11 | GPIO_Pin_12, GPIO_Mode_AIN);
 * 
 * 2. 未使用的I2C引脚:
 *    GPIO_Config_Pin(GPIOB, GPIO_Pin_6 | GPIO_Pin_7, GPIO_Mode_AIN);
 * 
 * 3. 禁用JTAG（如不需要调试）:
 *    RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO, ENABLE);
 *    GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE);
 *    // 然后可以配置PB3, PB4, PB5为GPIO
 * 
 * 4. 关闭未使用的外设时钟:
 *    RCC_APB2PeriphClockCmd(RCC_APB2Periph_ADC1, DISABLE);
 *    RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO, DISABLE);
 */

/*******************************************************************************
 * 【引脚配置检查清单】
 ******************************************************************************/
/*
 * □ 所有未使用引脚已配置为模拟输入模式
 * □ 所有使用引脚由对应模块正确初始化
 * □ 调试接口引脚未配置（保留SWD功能）
 * □ USB引脚根据需求配置（不使用USB时配置为AIN）
 * □ 时钟配置正确（GPIO时钟已使能）
 * □ 无引脚冲突（同一引脚未被多个模块配置）
 * □ 文档已更新（新增引脚使用时）
 */

/* 
 * === 配置完成 ===
 * 所有未使用的GPIO引脚已正确配置为模拟输入模式，
 * 系统功耗降至最低，引脚不会悬空干扰其他电路。
 */
